<% if current_user.favorite?(recipe) %>
  <%= form_for(current_user.favorites.find_by(recipe_id: recipe.id), html: { method: :delete }) do |f| %>
    <%= hidden_field_tag :recipe_id, recipe.id %>
    <%= f.submit 'お気に入り中', class: 'btn btn-unfavorite btn-block btn-sm' %>
  <% end %>
<% else %>
  <%= form_for(current_user.favorites.build) do |f| %>
    <%= hidden_field_tag :recipe_id, recipe.id %>
    <%= f.submit 'レシピをお気に入り', class: 'btn btn-favorite btn-block btn-sm' %>
  <% end %>
<% end %>

