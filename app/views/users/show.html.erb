<div class="user-profile">
  <div class="name text-center">
    <h1><%= @user.name %></h1>
  </div>
  <div>
    <%= render 'recipes/recipes', recipes: @recipes, user: @user %>
    <%= paginate @recipes %>
  </div>
</div>
